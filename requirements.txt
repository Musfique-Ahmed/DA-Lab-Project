# Credit Risk Intelligence — Phase 1 dependencies
# Pinned to compatible-with-Python-3.14 major versions.
# Phase 4 adds torch for the MLP (chosen over tensorflow because TF has no cp314 wheels).

# --- Core data handling ---
pandas>=2.2,<3
numpy>=2.0,<3

# --- Visualization (EDA in Phase 2) ---
matplotlib>=3.9,<4
seaborn>=0.13,<1
plotly>=5.22,<6

# --- Modeling (Phases 3-4) ---
scikit-learn>=1.5,<2
xgboost>=2.0,<3          # chosen gradient-boosting library
shap>=0.46,<1            # interpretability

# --- Class-imbalance handling (Phase 4) ---
imbalanced-learn>=0.12,<1

# --- Deep learning (Phase 4 MLP) ---
# PyTorch chosen because TensorFlow has no cp314 (Python 3.14) wheels.
torch>=2.11,<3

# --- Dashboard (Phase 5) ---
# Pin to <1.50: Streamlit 1.50+ migrated to an ASGI stack (uvicorn +
# starlette) that has a WebSocket-after-handshake regression on Python
# 3.14, leaving the browser stuck on skeleton placeholders forever.
# Tested working version: 1.49.1 (Tornado-WSGI).
streamlit>=1.38,<1.50

# --- Testing ---
pytest>=8.3,<9

# --- Notebook execution (Phase 2 needs to run the EDA notebook) ---
jupyter>=1.1,<2
nbformat>=5.10,<6
nbconvert>=7.16,<8
ipykernel>=6.29,<7

# --- Persistence (parquet + joblib) ---
# pyarrow pinned loosely: 17.x predates Python 3.14 wheels; allow 18+ for 3.14 support.
pyarrow>=17.0,<23
joblib>=1.4,<2
